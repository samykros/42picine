#include <stdio.h>
#include <stdlib.h>
/*
int main(int argc, char *argv[]) 
{
  // Check if at least one command-line argument is provided
  if (argc < 2) 
  {
    printf("Usage: %s <integer>\n", argv[0]);
    return 1;
  }

  // Convert the first command-line argument to an integer using atoi
  int num = atoi(argv[1]);

  // Add 1 to the integer
  num += 1;

  // Print the result
  printf("The result is: %d\n", num);

  return 0;
}
*/

int main()
{
	int x = 300000000000;
	printf("%d", x);
	return (0);
}
